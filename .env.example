# Binance MCP 服务器环境变量配置文件模板
# 复制此文件为 .env 并填入您的实际配置

# =================================
# Binance API 配置 (必需)
# =================================
# 从 https://binance.com/api-management 获取API密钥
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

# 是否使用测试网 (true/false)
# 测试网用于开发和测试，不涉及真实资金
BINANCE_TESTNET=false

# =================================
# HTTP 服务器配置
# =================================
# 服务器运行模式 (stdio/http)
# stdio: 用于Claude Desktop本地连接
# http: 用于远程HTTP SSE连接
SERVER_MODE=stdio

# HTTP服务器端口 (仅在http模式下使用)
PORT=3000

# HTTP服务器主机地址 (仅在http模式下使用)
# 0.0.0.0 表示监听所有网卡
# localhost 表示仅本地访问
HOST=0.0.0.0

# =================================
# 日志配置
# =================================
# 日志级别 (debug/info/warn/error)
LOG_LEVEL=info

# =================================
# 可选配置 (生产环境部署时使用)
# =================================
# API请求超时时间 (毫秒)
API_TIMEOUT=10000

# CORS允许的源 (逗号分隔，* 表示允许所有)
ALLOWED_ORIGINS=*

# 健康检查间隔 (秒)
HEALTH_CHECK_INTERVAL=30